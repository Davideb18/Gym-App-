// Servizio per la gestione dei media degli esercizi
// In futuro qui integreremo il nuovo provider video open-source

export const getExerciseVideoUrl = async (exerciseId: string): Promise<string | null> => {
  // Per ora restituiamo null finché non scegliamo il nuovo provider
  return null;
};
